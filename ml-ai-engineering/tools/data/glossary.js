// Master glossary — source of truth for the Glossary tool and Flashcards tool.
// Every term introduced anywhere in the curriculum belongs here.
// When a lesson introduces a new term, add it to this file in the same change.
//
// Record shape:
//   term               — display name
//   key                — stable identifier (kebab-case); matches `data-term` in lesson tooltips
//   module             — "module-01" ... "module-08" (where the term is introduced)
//   lesson             — "lesson-01" ... "lesson-NN" within that module
//   lessonPath         — relative path (from tools/) to the lesson that introduces the term
//   category           — grouping for the glossary UI ("Linear Algebra", "Calculus", etc.)
//   short              — 1-line definition for the glossary card and inline tooltips
//   long               — longer HTML explanation for the glossary expanded view
//   example            — HTML snippet showing the term in use (usually code)
//   related            — array of other term keys worth cross-linking
//   flashcardFront     — the question/prompt side of the flashcard
//   flashcardBack      — the answer side (can be richer than the `short`)

window.ML_GLOSSARY = [
  // ===================== MODULE 1 — Linear Algebra & Calculus =====================
  {
    term: "Vector",
    key: "vector",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "An ordered list of numbers, representing one thing described by several measurements.",
    long: "<p>A <strong>vector</strong> is an ordered list of numbers. That is the entire definition. It shows up everywhere in ML because almost any real-world thing (a user, a document, a pixel, a word) can be described by a fixed list of measurements, and once you have a list of numbers you can do math on it.</p><p>The order matters: swapping positions changes the meaning. In code, a vector is an <code>np.array([...])</code> with shape <code>(n,)</code>.</p>",
    example: "<pre><code>u = np.array([34, 487.50, 120])  # age, spend, days\nu.shape  # (3,)</code></pre>",
    related: ["feature", "feature-vector", "sample", "dimension", "matrix"],
    flashcardFront: "What is a vector?",
    flashcardBack: "An ordered list of numbers, stored in a fixed order. In ML, usually represents one 'thing' (user, document, pixel) described by several measurements.",
  },
  {
    term: "Feature",
    key: "feature",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "A single measurable property of a thing. Columns of your data matrix.",
    long: "<p>A <strong>feature</strong> is one measurable property of a sample. <code>age</code> is a feature; <code>zip_code</code> is a feature; <code>pixel_brightness_at_x_y</code> is a feature. In a data matrix <code>X</code> of shape <code>(n_samples, n_features)</code>, each column corresponds to one feature.</p><p>Choosing and shaping features (\"feature engineering\") is a substantial part of the ML engineer's job — covered in Module 4.</p>",
    example: "",
    related: ["feature-vector", "sample", "matrix"],
    flashcardFront: "What is a feature?",
    flashcardBack: "A single measurable property of a thing. In an ML data matrix, columns are features; rows are samples.",
  },
  {
    term: "Feature vector / Sample / Data point",
    key: "feature-vector",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "The full list of feature values for one sample. Three synonyms.",
    long: "<p>The <strong>feature vector</strong> for one entity is the vector containing all its feature values in a fixed order. Synonymous with <strong>sample</strong>, <strong>data point</strong>, and (in some docs) <strong>example</strong>. All four terms appear in the literature; they all mean the same thing — one row of your data matrix.</p>",
    example: "",
    related: ["feature", "sample", "vector"],
    flashcardFront: "What's another name for a 'feature vector'?",
    flashcardBack: "Sample, data point, or example — all four terms mean the same thing: one row of your data matrix, containing one entity's feature values.",
  },
  {
    term: "Sample",
    key: "sample",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "One data point. One row of your X matrix.",
    long: "<p>A <strong>sample</strong> is a single data point in your dataset — one row of <code>X</code>. It is one entity the model will make a prediction about. Synonymous with \"feature vector,\" \"data point,\" and \"example.\"</p><p>Note: \"sample\" in ML is <em>not</em> the same as \"sample\" in statistics (where it often means a subset drawn from a larger population). Context disambiguates.</p>",
    example: "",
    related: ["feature-vector", "feature", "matrix"],
    flashcardFront: "What is a sample in ML?",
    flashcardBack: "One data point — one row of your data matrix. The thing the model will make a prediction about. Synonymous with 'data point' or 'example'.",
  },
  {
    term: "Dimension",
    key: "dimension",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "The count of numbers in a vector.",
    long: "<p>The <strong>dimension</strong> of a vector is how many numbers it contains. A 3D vector has 3 components; a 300-dimensional word embedding has 300.</p><p>Note: in ML, \"300-dimensional\" doesn't imply any physical or spatial meaning — it just means 300 numbers. Don't try to visualize beyond 3D; lean on the math instead.</p>",
    example: "",
    related: ["vector", "tensor"],
    flashcardFront: "What does 'dimension' mean for a vector in ML?",
    flashcardBack: "The count of numbers the vector contains. A 300-dimensional embedding is just 300 numbers; no physical space implied.",
  },
  {
    term: "Origin",
    key: "origin",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "The all-zeros point where vector arrows begin.",
    long: "<p>The <strong>origin</strong> is the point where every coordinate is zero: <code>(0, 0)</code> in 2D, <code>(0, 0, 0)</code> in 3D, and so on. In the geometric picture of vectors, every vector is drawn as an arrow starting at the origin and ending at the point matching its components.</p>",
    example: "",
    related: ["vector"],
    flashcardFront: "What is the origin?",
    flashcardBack: "The point where every coordinate is zero. Vectors are drawn as arrows starting at the origin.",
  },
  {
    term: "Element-wise operation",
    key: "element-wise",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "An operation applied position-by-position, no mixing across positions.",
    long: "<p><strong>Element-wise</strong> means the operation is applied at each corresponding position independently: position 0 with position 0, position 1 with position 1, and so on. Contrast with matrix multiplication, which mixes across positions.</p><p>In NumPy, <code>+</code>, <code>-</code>, <code>*</code>, <code>/</code> are all element-wise. <code>@</code> is matrix multiplication.</p>",
    example: "<pre><code>[1, 2, 3] + [4, 5, 6]  # element-wise → [5, 7, 9]</code></pre>",
    related: ["matmul", "broadcasting"],
    flashcardFront: "What is an element-wise operation?",
    flashcardBack: "An operation applied position-by-position with no mixing across positions. <code>[1,2]+[3,4] = [4,6]</code>. In NumPy, <code>+ - * /</code> are element-wise; <code>@</code> is not.",
  },
  {
    term: "Scalar",
    key: "scalar",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "A single number (not a vector or matrix).",
    long: "<p>A <strong>scalar</strong> is just a single number — not a vector, not a matrix. In code, an ordinary <code>int</code> or <code>float</code>. \"Scalar multiplication\" means multiplying every component of a vector by the same number; geometrically, it stretches or shrinks the arrow.</p>",
    example: "<pre><code>2 * np.array([3, 1])  # array([6, 2])</code></pre>",
    related: ["vector", "tensor"],
    flashcardFront: "What is a scalar?",
    flashcardBack: "A single number. Not a vector, not a matrix. In code: an <code>int</code> or <code>float</code>.",
  },
  {
    term: "Dot product",
    key: "dot-product",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "Multiply two vectors pairwise, sum the products. Measures directional alignment.",
    long: "<p>The <strong>dot product</strong> of two vectors returns a single number: multiply corresponding components pairwise, then add them up. It measures how aligned the two vectors are — large positive means similar direction, zero means perpendicular, negative means opposite directions.</p><p>This is the single most common operation in ML. Linear regression output, attention scores in transformers, and cosine similarity all build on dot products.</p>",
    example: "<pre><code>a = np.array([3, 1])\nb = np.array([2, 4])\na @ b  # 3*2 + 1*4 = 10</code></pre>",
    related: ["alignment", "cosine-similarity", "linear-regression", "attention"],
    flashcardFront: "How do you compute a dot product, and what does it measure?",
    flashcardBack: "Multiply corresponding components of two vectors, then sum the products. Returns a scalar. Measures directional alignment: positive = similar direction, zero = perpendicular, negative = opposite.",
  },
  {
    term: "Alignment",
    key: "alignment",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "How similarly two vectors point, measured by the dot product.",
    long: "<p><strong>Alignment</strong> is an informal term for how similarly two vectors point in space. Measured numerically by the dot product (absolute), or by cosine similarity (scale-invariant, in [-1, 1]).</p>",
    example: "",
    related: ["dot-product", "cosine-similarity"],
    flashcardFront: "How is alignment between two vectors measured?",
    flashcardBack: "By the dot product (absolute) or cosine similarity (normalized, range −1 to +1). Positive = similar direction, zero = perpendicular, negative = opposite.",
  },
  {
    term: "Cosine similarity",
    key: "cosine-similarity",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "Normalized dot product; ranges from −1 to +1.",
    long: "<p><strong>Cosine similarity</strong> is the dot product of two vectors after both have been normalized to length 1. It ranges from −1 (opposite directions) to +1 (same direction), with 0 meaning perpendicular.</p><p>It's the default similarity metric for embeddings and document retrieval because it's invariant to vector magnitude — two vectors with the same direction but different lengths have cosine similarity 1.</p>",
    example: "<pre><code>from numpy.linalg import norm\ncos = (a @ b) / (norm(a) * norm(b))</code></pre>",
    related: ["dot-product", "unit-vector", "normalize-vec"],
    flashcardFront: "What is cosine similarity and what's its range?",
    flashcardBack: "Dot product after normalizing both vectors to length 1. Range: −1 (opposite) to +1 (same direction). Invariant to magnitude. Workhorse metric for semantic search and embeddings.",
  },
  {
    term: "Attention",
    key: "attention",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Deep Learning (forward ref)",
    short: "Mechanism where the model computes dot products to decide what to 'look at'.",
    long: "<p><strong>Attention</strong> is a neural-network mechanism where, for each position in a sequence, the model computes dot products against every other position to decide what to pay attention to. It's the core building block of the transformer architecture.</p><p>Covered in depth in Module 6 — named here because attention scores are dot products, making them a Module 1 concept in disguise.</p>",
    example: "",
    related: ["transformer", "dot-product"],
    flashcardFront: "What operation is at the heart of attention?",
    flashcardBack: "Dot products between 'query' and 'key' vectors. For every position in a sequence, attention scores against every other position are computed this way.",
  },
  {
    term: "Transformer",
    key: "transformer",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Deep Learning (forward ref)",
    short: "The neural-network architecture behind GPT, BERT, and Claude.",
    long: "<p>The <strong>transformer</strong> is the neural-network architecture that powers GPT, BERT, Claude, and most modern large language models. It's built around the attention mechanism. Full treatment in Module 6.</p>",
    example: "",
    related: ["attention"],
    flashcardFront: "What architecture powers GPT, BERT, and Claude?",
    flashcardBack: "The transformer architecture, built around the attention mechanism (which is itself built on dot products).",
  },
  {
    term: "Linear regression",
    key: "linear-regression",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Classical ML (forward ref)",
    short: "Simplest ML model: y_pred = X @ w.",
    long: "<p><strong>Linear regression</strong> predicts a numeric output as a dot product of input features with a learned weight vector: <code>y_pred = X @ w</code>. Every linear model you'll encounter (ordinary least squares, ridge, lasso) is this operation with different ways of choosing <code>w</code>.</p><p>We build this from scratch in Module 3.</p>",
    example: "<pre><code>y_pred = X @ w  # shape (n_samples,)</code></pre>",
    related: ["dot-product", "matmul"],
    flashcardFront: "Write the prediction formula for linear regression.",
    flashcardBack: "<code>y_pred = X @ w</code> — a matrix-vector product (or dot product per row) of feature matrix <code>X</code> with learned weight vector <code>w</code>.",
  },
  {
    term: "Norm",
    key: "norm",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "The length of a vector.",
    long: "<p>The <strong>norm</strong> of a vector — written <code>||v||</code> — is its length. Several norms exist; the default is the L2 norm: <code>sqrt(v1² + v2² + ... + vn²)</code>. Other norms (L1, L∞) capture different notions of length and show up in different regularization techniques.</p>",
    example: "<pre><code>np.linalg.norm(np.array([3, 4]))  # 5.0</code></pre>",
    related: ["l2-norm", "unit-vector", "normalize-vec"],
    flashcardFront: "What does 'norm of a vector' mean, and how do you compute the L2 norm?",
    flashcardBack: "Norm = vector length. L2 norm = <code>√(v₁² + v₂² + … + vₙ²)</code> — Pythagoras generalized. In NumPy: <code>np.linalg.norm(v)</code>.",
  },
  {
    term: "L2 norm (Euclidean norm)",
    key: "l2-norm",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "Default vector length: √(v₁² + v₂² + …).",
    long: "<p>The <strong>L2 norm</strong> — also called the Euclidean norm — is the default vector length formula: <code>sqrt(v1² + v2² + ... + vn²)</code>. It generalizes the Pythagorean theorem to any number of dimensions. Used for distances, normalization, and L2 regularization (ridge regression).</p>",
    example: "<pre><code>np.linalg.norm(v, ord=2)  # L2 norm (default)</code></pre>",
    related: ["norm", "unit-vector"],
    flashcardFront: "What's the L2 norm formula?",
    flashcardBack: "<code>||v||₂ = √(v₁² + v₂² + … + vₙ²)</code>. Same as the Euclidean distance from the origin. Default in NumPy's <code>np.linalg.norm</code>.",
  },
  {
    term: "Unit vector",
    key: "unit-vector",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "A vector with length 1.",
    long: "<p>A <strong>unit vector</strong> has length (norm) 1. Useful when only direction matters, not magnitude. Obtained by dividing any non-zero vector by its norm. Cosine similarity is effectively the dot product of two unit vectors.</p>",
    example: "<pre><code>v / np.linalg.norm(v)  # unit vector, same direction as v</code></pre>",
    related: ["norm", "normalize-vec", "cosine-similarity"],
    flashcardFront: "What is a unit vector and how do you produce one?",
    flashcardBack: "A vector with length 1. Obtained by dividing any non-zero vector by its norm: <code>v / np.linalg.norm(v)</code>.",
  },
  {
    term: "Normalize (vector)",
    key: "normalize-vec",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "Divide a vector by its norm so it has length 1.",
    long: "<p>To <strong>normalize</strong> a vector (in the linear-algebra sense) is to divide it by its norm, producing a unit vector with the same direction. Different from \"normalizing data\" in statistics, which usually means rescaling features to zero mean and unit variance.</p>",
    example: "",
    related: ["norm", "unit-vector"],
    flashcardFront: "What does 'normalize a vector' mean?",
    flashcardBack: "Divide the vector by its norm, producing a unit vector (length 1) in the same direction. Different from statistical normalization (zero-mean, unit-variance rescaling).",
  },
  {
    term: "Matrix",
    key: "matrix",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "A 2D grid of numbers. Your ML dataset lives here.",
    long: "<p>A <strong>matrix</strong> is a 2D grid of numbers with <code>m</code> rows and <code>n</code> columns (written <code>m × n</code>). In ML, your feature data is a matrix called <code>X</code> of shape <code>(n_samples, n_features)</code> — rows are samples, columns are features.</p>",
    example: "<pre><code>A = np.array([[1, 2, 3],\n              [4, 5, 6]])\nA.shape  # (2, 3)</code></pre>",
    related: ["vector", "tensor", "transpose", "matmul"],
    flashcardFront: "What shape is a typical ML data matrix <code>X</code>?",
    flashcardBack: "<code>(n_samples, n_features)</code> — rows are samples, columns are features. Every sklearn function expects this shape.",
  },
  {
    term: "scikit-learn",
    key: "scikit-learn",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Tooling",
    short: "The standard classical-ML library in Python.",
    long: "<p><strong>scikit-learn</strong> (imported as <code>sklearn</code>) is the standard classical-ML library in Python. Every model in it follows the same uniform API: <code>.fit(X, y)</code> to train, <code>.predict(X)</code> to predict. This uniformity makes swapping models trivial. Used extensively in Module 3.</p>",
    example: "<pre><code>from sklearn.linear_model import LinearRegression\nmodel = LinearRegression().fit(X, y)\ny_pred = model.predict(X_new)</code></pre>",
    related: [],
    flashcardFront: "What's the standard API pattern in scikit-learn?",
    flashcardBack: "<code>.fit(X, y)</code> to train, <code>.predict(X)</code> to predict. Every model in sklearn follows this same interface, which makes swapping models trivial.",
  },
  {
    term: "Transpose",
    key: "transpose",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "Flip a matrix: rows become columns.",
    long: "<p>The <strong>transpose</strong> of a matrix swaps rows and columns — <code>A[i][j]</code> becomes <code>A.T[j][i]</code>. Shape <code>(m, n)</code> becomes <code>(n, m)</code>. Transpose is the standard tool for reshaping matrices so operations line up.</p>",
    example: "<pre><code>A.T       # transpose\nA.T.T     # back to A</code></pre>",
    related: ["matrix", "matmul"],
    flashcardFront: "What does A.T do, and how does it change the shape?",
    flashcardBack: "Transposes the matrix — rows become columns, <code>A[i][j] → A.T[j][i]</code>. Shape <code>(m, n)</code> becomes <code>(n, m)</code>.",
  },
  {
    term: "Matrix multiplication (matmul)",
    key: "matmul",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "Each result entry is a dot product of a row of A with a column of B.",
    long: "<p><strong>Matrix multiplication</strong> (matmul) computes <code>A @ B</code> where entry <code>[i][j]</code> of the result is the dot product of row <code>i</code> of <code>A</code> with column <code>j</code> of <code>B</code>. For shapes: <code>(m, n) @ (n, p) → (m, p)</code>; the inner dimensions must match.</p><p>In NumPy, <code>@</code> is matmul; <code>*</code> is element-wise. Mixing them up is the #1 NumPy bug. Matmul is also non-commutative: <code>A @ B ≠ B @ A</code>.</p>",
    example: "<pre><code>A @ B   # matrix multiplication\nA * B   # element-wise — DIFFERENT</code></pre>",
    related: ["dot-product", "inner-dimensions", "element-wise", "transpose"],
    flashcardFront: "For <code>A @ B</code> to be defined, what must match — and what's the output shape?",
    flashcardBack: "The inner dimensions must match: <code>A.shape[1] == B.shape[0]</code>. Output shape is <code>(A.shape[0], B.shape[1])</code>.",
  },
  {
    term: "Inner dimensions",
    key: "inner-dimensions",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "For (m × n) @ (n × p), the two n's. Must match.",
    long: "<p>In matrix multiplication <code>(m × n) @ (n × p)</code>, the two <code>n</code>s are the <strong>inner dimensions</strong>. They must be equal for matmul to be defined. The outer dimensions <code>m</code> and <code>p</code> become the shape of the result.</p>",
    example: "",
    related: ["matmul"],
    flashcardFront: "In a matmul <code>(m × n) @ (n × p)</code>, which dimensions must match?",
    flashcardBack: "The two inner dimensions (both labeled <code>n</code>). The outer dimensions <code>m</code> and <code>p</code> become the result shape.",
  },
  {
    term: "Linear combination",
    key: "linear-combination",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "A weighted sum of vectors: c₁v₁ + c₂v₂ + …",
    long: "<p>A <strong>linear combination</strong> of vectors is a weighted sum: <code>c₁·v₁ + c₂·v₂ + ...</code> where the <code>cᵢ</code> are scalars. Matrix-vector multiplication <code>A @ v</code> is a linear combination of <code>A</code>'s columns, with the weights being the components of <code>v</code>. This view is central to understanding PCA and eigenvectors.</p>",
    example: "",
    related: ["matmul", "scalar", "eigenvalues", "pca"],
    flashcardFront: "What is <code>A @ v</code>, viewed as a linear combination?",
    flashcardBack: "A weighted sum of <code>A</code>'s columns, where the weights are the components of <code>v</code>. <code>v[0]</code> times column 0 plus <code>v[1]</code> times column 1 plus …",
  },
  {
    term: "Eigenvalue",
    key: "eigenvalues",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A scalar describing how much a matrix stretches along a specific direction.",
    long: "<p>An <strong>eigenvalue</strong> <code>λ</code> of a square matrix <code>A</code> is a scalar such that, for some non-zero vector <code>v</code> (the eigenvector), <code>A @ v = λ · v</code>. Geometrically: <code>v</code> is a direction the matrix stretches without rotating, and <code>λ</code> is the amount it stretches by. Eigenvalues/eigenvectors are the mathematical core of PCA and dimensionality reduction.</p>",
    example: "<pre><code>eigenvalues, eigenvectors = np.linalg.eig(A)</code></pre>",
    related: ["pca", "matrix", "linear-combination"],
    flashcardFront: "What's the defining equation for an eigenvalue/eigenvector pair?",
    flashcardBack: "<code>A @ v = λ · v</code> — the matrix <code>A</code> applied to the eigenvector <code>v</code> produces the same vector scaled by the eigenvalue <code>λ</code>. Geometrically: <code>v</code> is a direction <code>A</code> stretches without rotating.",
  },
  {
    term: "PCA (Principal Component Analysis)",
    key: "pca",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Classical ML",
    short: "Reduce high-dimensional data to a few axes capturing most of the variance.",
    long: "<p><strong>PCA</strong> (Principal Component Analysis) reduces high-dimensional data to a lower-dimensional representation that preserves as much variance as possible. Built on eigenvalue decomposition of the covariance matrix (or equivalently, SVD of the centered data matrix).</p><p>Useful for visualization, noise reduction, and speeding up downstream models. Used throughout Module 4.</p>",
    example: "<pre><code>from sklearn.decomposition import PCA\npca = PCA(n_components=2).fit_transform(X)</code></pre>",
    related: ["eigenvalues", "linear-combination"],
    flashcardFront: "What problem does PCA solve, and what linear-algebra concept does it rely on?",
    flashcardBack: "Reduces high-dimensional data to a few axes (principal components) that capture most of the variance. Built on eigenvalue decomposition of the covariance matrix (equivalently, SVD of the centered data).",
  },
  {
    term: "Tensor",
    key: "tensor",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "An n-dimensional array.",
    long: "<p>A <strong>tensor</strong> is an n-dimensional array. Scalar = 0D tensor, vector = 1D, matrix = 2D. Deep nets use 3D, 4D, and higher tensors — for example, a batch of images as <code>(batch, channels, height, width)</code>.</p><p>PyTorch and TensorFlow are both named around tensors because they're the universal data container for deep learning.</p>",
    example: "",
    related: ["scalar", "vector", "matrix", "batch"],
    flashcardFront: "What is a tensor, and what shape might a batch of images have?",
    flashcardBack: "An n-dimensional array (scalar = 0D, vector = 1D, matrix = 2D, higher = tensors). A batch of images: <code>(batch, channels, height, width)</code> — rank 4.",
  },
  {
    term: "Broadcasting",
    key: "broadcasting",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "NumPy's rule for operating on arrays of different but compatible shapes.",
    long: "<p><strong>Broadcasting</strong> is NumPy's rule for doing element-wise operations on arrays of different but compatible shapes without explicit loops. The smaller array is conceptually \"stretched\" along missing or length-1 axes to match the larger one. If the shapes can't be aligned unambiguously, it raises an error.</p>",
    example: "<pre><code>X - X.mean(axis=0)  # (1000,5) - (5,) → (1000,5)</code></pre>",
    related: ["element-wise", "bias-vector"],
    flashcardFront: "What does NumPy broadcasting do?",
    flashcardBack: "Lets you do element-wise operations on arrays of different shapes by stretching the smaller along missing or length-1 axes. Aligns from the rightmost axis. Errors if shapes can't be unambiguously aligned.",
  },
  {
    term: "Bias vector",
    key: "bias-vector",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Linear Algebra",
    short: "A constant vector added to every output of a layer.",
    long: "<p>A <strong>bias vector</strong> is a constant vector added to every output of a neural-network layer (or every row of a batched computation). The <code>+ b</code> in the model formula <code>y = Wx + b</code>. Gives the model flexibility to shift predictions independently of the input.</p>",
    example: "<pre><code>output = X @ W + bias  # broadcast adds bias to every row</code></pre>",
    related: ["broadcasting"],
    flashcardFront: "What's the role of the bias vector in <code>y = Wx + b</code>?",
    flashcardBack: "Shifts predictions independently of the input. Added element-wise to every output of a layer (or every row of a batched computation), usually via broadcasting.",
  },
  {
    term: "Batch",
    key: "batch",
    module: "module-01",
    lesson: "lesson-01",
    lessonPath: "../module-01-math-foundations/lesson-01-vectors-and-matrices.html",
    category: "Deep Learning",
    short: "A group of samples processed together.",
    long: "<p>A <strong>batch</strong> is a group of samples processed together in one forward/backward pass. Shape <code>(batch_size, n_features)</code> for tabular data, <code>(batch_size, channels, H, W)</code> for images. Batching is how GPUs reach their throughput — processing one sample at a time leaves most of the hardware idle.</p>",
    example: "",
    related: ["tensor", "sample"],
    flashcardFront: "What is a batch and why does it matter?",
    flashcardBack: "A group of samples processed together in one forward/backward pass. GPUs reach full throughput only with decent batch sizes; single-sample inference leaves hardware idle.",
  },

  // ===================== MODULE 1 — Linear Algebra & Calculus (Lesson 2) =====================
  {
    term: "Linear transformation",
    key: "linear-transformation",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A function from vectors to vectors that keeps the origin fixed and straight grid lines straight.",
    long: "<p>A <strong>linear transformation</strong> is a function from vectors to vectors with two properties: (1) it keeps the origin at the origin, and (2) it sends straight, evenly spaced grid lines to straight, evenly spaced grid lines. Equivalently: it preserves addition and scalar multiplication — <code>T(u + v) = T(u) + T(v)</code> and <code>T(c·v) = c·T(v)</code>.</p><p>Every matrix defines a linear transformation (and vice versa). Reading <code>A @ v</code> as \"apply the transformation <code>A</code> to the vector <code>v</code>\" is the key perspective shift of this lesson — matrices are <em>functions</em>, not just data tables.</p>",
    example: "<pre><code>A = np.array([[2, 1],\n              [0, 1]])  # a shear\nA @ np.array([1, 0])    # array([2, 0]) — where î lands</code></pre>",
    related: ["matrix", "matmul", "basis-vector", "affine", "origin"],
    flashcardFront: "What two properties define a linear transformation?",
    flashcardBack: "(1) It fixes the origin, and (2) it sends straight, evenly spaced grid lines to straight, evenly spaced grid lines. Equivalently: it preserves addition and scalar multiplication. Every matrix defines one.",
  },
  {
    term: "Affine transformation",
    key: "affine",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A linear transformation plus a translation: x ↦ Wx + b.",
    long: "<p>An <strong>affine transformation</strong> is a linear transformation followed by a constant shift: <code>x ↦ Wx + b</code>. The <code>+ b</code> is the only difference from strictly linear — it lets the transformation move the origin. Pure linear transformations must fix the origin; affine ones don't have to.</p><p>Every neural-network layer is affine before the nonlinearity: <code>y = Wx + b</code>, then a ReLU or similar. The bias vector <code>b</code> is what makes it affine rather than linear.</p>",
    example: "<pre><code>y = W @ x + b   # affine layer, pre-activation</code></pre>",
    related: ["linear-transformation", "bias-vector", "matmul", "relu"],
    flashcardFront: "What's the difference between a linear and an affine transformation?",
    flashcardBack: "Affine = linear + translation: <code>x ↦ Wx + b</code>. The added constant <code>b</code> lets affine transformations move the origin, which pure linear ones can't. Neural-net layers are affine.",
  },
  {
    term: "Basis vector",
    key: "basis-vector",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "One of the standard unit directions everything else is built from — in 2D, î and ĵ.",
    long: "<p>A <strong>basis vector</strong> is one of the standard unit vectors that every other vector is expressed in terms of. In 2D: <code>î = (1, 0)</code> and <code>ĵ = (0, 1)</code>. Any vector <code>(a, b)</code> is really <code>a·î + b·ĵ</code> — a linear combination of the basis.</p><p>The magic trick of this lesson: the columns of a matrix are exactly where the basis vectors land after the transformation. So to know what a matrix does, you only need to track where î and ĵ go.</p>",
    example: "<pre><code>A = np.array([[2, 1],\n              [0, 3]])\n# î lands at (2, 0) — column 0\n# ĵ lands at (1, 3) — column 1</code></pre>",
    related: ["vector", "linear-combination", "linear-transformation", "matrix"],
    flashcardFront: "Where do the columns of a matrix come from, geometrically?",
    flashcardBack: "They're where the basis vectors land after the transformation. Column 0 = <code>A @ î</code>, column 1 = <code>A @ ĵ</code>, etc. That's why knowing where î and ĵ go tells you the whole matrix.",
  },
  {
    term: "Identity matrix",
    key: "identity-matrix",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "The 'do-nothing' matrix: 1s on the diagonal, 0s elsewhere. Iv = v.",
    long: "<p>The <strong>identity matrix</strong> <code>I</code> has 1s on the main diagonal and 0s everywhere else. It's the identity for matrix multiplication: <code>I @ v = v</code> and <code>I @ A = A @ I = A</code>. Geometrically, it's the transformation that does nothing — î stays at î, ĵ stays at ĵ.</p><p>Comes up constantly: as a starting point for iterative algorithms, as the right-hand side when solving <code>A @ X = I</code> to find <code>A</code>'s inverse, and as the thing orthogonal matrices satisfy in <code>Q^T Q = I</code>.</p>",
    example: "<pre><code>np.eye(3)\n# [[1, 0, 0],\n#  [0, 1, 0],\n#  [0, 0, 1]]</code></pre>",
    related: ["diagonal-matrix", "inverse-matrix", "matmul"],
    flashcardFront: "What is the identity matrix, and what does it do?",
    flashcardBack: "A square matrix with 1s on the diagonal and 0s elsewhere. <code>I @ v = v</code> and <code>I @ A = A</code>. In NumPy: <code>np.eye(n)</code>. The 'do-nothing' transformation.",
  },
  {
    term: "Diagonal matrix",
    key: "diagonal-matrix",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A matrix whose only nonzero entries lie on the main diagonal — pure per-axis scaling.",
    long: "<p>A <strong>diagonal matrix</strong> has nonzero entries only on its main diagonal. It represents independent scaling along each axis: <code>diag(2, 3) @ [x, y] = [2x, 3y]</code>. Geometrically, it stretches or shrinks each axis separately without rotating or shearing.</p><p>Sklearn's <code>StandardScaler</code> effectively applies a diagonal matrix to your features — each feature gets its own scale factor. The <code>Σ</code> in SVD is also diagonal.</p>",
    example: "<pre><code>D = np.diag([2, 3])\nD @ np.array([1, 1])   # [2, 3]</code></pre>",
    related: ["identity-matrix", "svd", "matrix"],
    flashcardFront: "What does a diagonal matrix do geometrically?",
    flashcardBack: "Scales each axis independently — no rotation, no shear. <code>diag(a, b) @ [x, y] = [ax, by]</code>. <code>StandardScaler</code> effectively applies a diagonal matrix to your features.",
  },
  {
    term: "Shear",
    key: "shear",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A transformation that slides points parallel to an axis, proportional to their distance from it.",
    long: "<p>A <strong>shear</strong> is a linear transformation that slides points parallel to an axis by an amount proportional to their distance from it. Squares become parallelograms; vertical lines stay vertical (for a horizontal shear) but get tilted. Crucially, a shear preserves area — its determinant is 1.</p><p>Visually memorable and useful for building intuition about how a 2×2 matrix can distort space without collapsing it.</p>",
    example: "<pre><code>S = np.array([[1, 1],\n              [0, 1]])   # horizontal shear\nS @ np.array([0, 1])   # [1, 1] — ĵ tilts right</code></pre>",
    related: ["linear-transformation", "determinant", "matrix"],
    flashcardFront: "What does a shear do, and what is its determinant?",
    flashcardBack: "Slides points parallel to an axis by an amount proportional to their distance from that axis. Turns squares into parallelograms. Determinant = 1, so area is preserved.",
  },
  {
    term: "Determinant",
    key: "determinant",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A scalar measuring the signed area/volume scaling factor of a matrix transformation.",
    long: "<p>The <strong>determinant</strong> of a square matrix is a single scalar that tells you the signed area (2D) or volume (3D+) scaling factor of its transformation. <code>det = 2</code> means the matrix doubles areas; <code>det = −1</code> means it preserves area but flips orientation; <code>det = 0</code> means it collapses space to a lower dimension.</p><p>The zero case is the one you care about in ML: a zero determinant means the matrix is singular and not invertible. <code>X^T X</code> having zero determinant is why OLS breaks when features are collinear.</p>",
    example: "<pre><code>np.linalg.det(np.array([[2, 0],\n                        [0, 3]]))  # 6.0</code></pre>",
    related: ["singular-matrix", "inverse-matrix", "orientation", "collinearity"],
    flashcardFront: "What does the determinant of a matrix tell you?",
    flashcardBack: "The signed area/volume scaling factor of its transformation. <code>|det|</code> = how much it stretches area; sign = orientation (positive = preserved, negative = flipped); 0 = collapses to a lower dimension (singular).",
  },
  {
    term: "Orientation",
    key: "orientation",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "Whether a transformation preserves (det > 0) or flips (det < 0) the handedness of the axes.",
    long: "<p><strong>Orientation</strong> is whether the transformed coordinate axes still go counterclockwise from î to ĵ (positive determinant) or have been flipped to go clockwise (negative determinant, a reflection). It's the sign of the determinant.</p><p>Orientation-reversing transformations are reflections — mirror images. Reflections come up in SVD, where the orthogonal factors <code>U</code> and <code>V</code> can each be a rotation or a rotation-plus-reflection.</p>",
    example: "",
    related: ["determinant", "orthogonal"],
    flashcardFront: "What does the sign of the determinant tell you?",
    flashcardBack: "The orientation: positive determinant means the transformation preserves handedness (no flip), negative means it reverses it (a reflection is involved).",
  },
  {
    term: "Singular matrix",
    key: "singular-matrix",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A square matrix with determinant zero — not invertible.",
    long: "<p>A <strong>singular matrix</strong> is a square matrix whose determinant is zero. Equivalently: its columns are linearly dependent, it has no inverse, and its transformation collapses space onto a lower-dimensional subspace (a line, a plane), destroying information.</p><p>In ML, this shows up when features are collinear — then <code>X^T X</code> is singular, and OLS's closed-form <code>(X^T X)^{-1} X^T y</code> blows up. Ridge regression (adding <code>λI</code>) is one fix.</p>",
    example: "<pre><code>A = np.array([[1, 2],\n              [2, 4]])  # row 2 = 2·row 1\nnp.linalg.det(A)  # 0.0 — singular</code></pre>",
    related: ["determinant", "inverse-matrix", "rank-deficient", "collinearity", "ols"],
    flashcardFront: "What does it mean for a matrix to be singular?",
    flashcardBack: "Its determinant is zero: columns are linearly dependent, there's no inverse, and the transformation collapses space to a lower dimension. In ML, collinear features → singular <code>X^T X</code> → OLS breaks.",
  },
  {
    term: "Rank-deficient",
    key: "rank-deficient",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A matrix whose columns don't span as big a space as they could — some column is redundant.",
    long: "<p>A matrix is <strong>rank-deficient</strong> when its rank is less than the maximum possible (less than <code>min(m, n)</code>). Its columns don't span as big a space as they could — some column is a linear combination of the others, carrying no new information. For a square matrix, rank-deficient and singular are the same thing.</p><p>Diagnosing rank deficiency in your feature matrix is how you catch collinearity before it breaks training. <code>np.linalg.matrix_rank(X)</code> is the one-liner.</p>",
    example: "<pre><code>np.linalg.matrix_rank(X)  # drops below n_features when columns are dependent</code></pre>",
    related: ["rank", "singular-matrix", "collinearity"],
    flashcardFront: "What does 'rank-deficient' mean?",
    flashcardBack: "The matrix's columns don't span as big a space as they could — some column is a linear combination of the others. For a square matrix, same as singular. Indicates redundant features in an ML context.",
  },
  {
    term: "Collinearity",
    key: "collinearity",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "When two or more features are linear combinations of each other, breaking OLS.",
    long: "<p><strong>Collinearity</strong> (also \"multicollinearity\") is when two or more features in your data matrix are linear combinations of each other. Classic example: including both <code>age_years</code> and <code>age_months</code>, or both <code>height_cm</code> and <code>height_m</code>. The information is redundant.</p><p>Collinearity makes <code>X^T X</code> singular (or nearly so), which breaks the closed-form OLS solution. Catch it with <code>np.linalg.matrix_rank(X)</code> or a correlation heatmap before training.</p>",
    example: "",
    related: ["singular-matrix", "rank-deficient", "ols", "linear-combination"],
    flashcardFront: "What is collinearity and why does it matter?",
    flashcardBack: "When features are linear combinations of each other (redundant). Makes <code>X^T X</code> singular, so OLS's closed-form solution blows up. Diagnose with <code>matrix_rank</code> or a correlation heatmap.",
  },
  {
    term: "Inverse matrix",
    key: "inverse-matrix",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "The matrix A⁻¹ that undoes A: A⁻¹A = I. Exists only when det A ≠ 0.",
    long: "<p>The <strong>inverse</strong> of a square matrix <code>A</code> is the matrix <code>A⁻¹</code> that undoes it: <code>A⁻¹ @ A = A @ A⁻¹ = I</code>. Exists only when <code>A</code> is non-singular (<code>det A ≠ 0</code>).</p><p>You'll see <code>A⁻¹</code> all over the math (e.g. the OLS formula <code>w = (X^T X)⁻¹ X^T y</code>), but in code you almost never call <code>np.linalg.inv</code> — it's numerically unstable and wasteful. Use <code>np.linalg.solve(A, b)</code> instead; it gives you <code>A⁻¹ @ b</code> without forming the inverse explicitly.</p>",
    example: "<pre><code>np.linalg.inv(A)           # mathematically correct, numerically risky\nnp.linalg.solve(A, b)      # prefer this — solves A @ x = b</code></pre>",
    related: ["identity-matrix", "determinant", "singular-matrix", "ols"],
    flashcardFront: "What's the inverse of a matrix, and why avoid <code>np.linalg.inv</code>?",
    flashcardBack: "<code>A⁻¹</code> is the matrix that undoes <code>A</code>: <code>A⁻¹A = I</code>. Exists iff <code>det A ≠ 0</code>. Avoid <code>inv</code> because it's numerically unstable and slower than <code>np.linalg.solve(A, b)</code>.",
  },
  {
    term: "Projection matrix",
    key: "projection-matrix",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A matrix that collapses vectors onto a lower-dimensional subspace. Singular by design.",
    long: "<p>A <strong>projection matrix</strong> collapses vectors onto a lower-dimensional subspace — a line, a plane, etc. It's singular by construction (<code>det = 0</code>) because information is destroyed in the projection: many input vectors map to the same output.</p><p>This is what PCA does: project your high-dimensional data onto the top few principal-component axes. Embeddings and dimensionality reduction are all projections at heart.</p>",
    example: "<pre><code>P = np.array([[1, 0],\n              [0, 0]])   # projects onto the x-axis\nP @ np.array([3, 5])    # [3, 0]</code></pre>",
    related: ["singular-matrix", "determinant", "pca", "column-space"],
    flashcardFront: "Why is a projection matrix always singular?",
    flashcardBack: "Because it collapses space onto a lower-dimensional subspace — information is destroyed, so it can't be inverted. <code>det = 0</code>. PCA and embeddings are both projections.",
  },
  {
    term: "Column space",
    key: "column-space",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "The set of all vectors Xw for some w — all linear combinations of X's columns.",
    long: "<p>The <strong>column space</strong> of a matrix <code>X</code> is the set of all vectors you can write as <code>Xw</code> for some choice of <code>w</code>. Equivalently: all linear combinations of <code>X</code>'s columns. It's a subspace of the output space, and its dimension equals the matrix's rank.</p><p>Linear regression has a clean geometric interpretation in this language: finding the best <code>w</code> means projecting the target vector <code>y</code> onto the column space of <code>X</code>. The fitted values <code>ŷ = Xw</code> always live in that subspace.</p>",
    example: "",
    related: ["linear-combination", "matrix", "rank", "linear-regression"],
    flashcardFront: "What is the column space of a matrix X?",
    flashcardBack: "All vectors reachable as <code>Xw</code> for some <code>w</code> — equivalently, all linear combinations of <code>X</code>'s columns. Linear regression projects the target <code>y</code> onto this subspace.",
  },
  {
    term: "OLS (Ordinary Least Squares)",
    key: "ols",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Classical ML (forward ref)",
    short: "Closed-form linear regression fit: w = (XᵀX)⁻¹ Xᵀ y. Breaks when features are collinear.",
    long: "<p><strong>OLS</strong> (Ordinary Least Squares) is the classical closed-form solution to linear regression: <code>w = (XᵀX)⁻¹ Xᵀ y</code>. It's the exact <code>w</code> that minimizes the sum of squared residuals <code>‖Xw − y‖²</code>.</p><p>It fails when features are collinear — then <code>XᵀX</code> is singular and the inverse doesn't exist. That's where ridge regression (adds <code>λI</code>) and gradient-based methods step in. Implemented from scratch in Module 3.</p>",
    example: "<pre><code>w = np.linalg.solve(X.T @ X, X.T @ y)   # the OLS fit</code></pre>",
    related: ["linear-regression", "singular-matrix", "collinearity", "inverse-matrix"],
    flashcardFront: "Write the OLS closed-form solution for linear regression.",
    flashcardBack: "<code>w = (XᵀX)⁻¹ Xᵀ y</code>. The exact minimizer of <code>‖Xw − y‖²</code>. Breaks when <code>XᵀX</code> is singular (collinear features). Compute as <code>np.linalg.solve(X.T @ X, X.T @ y)</code>, not with <code>inv</code>.",
  },
  {
    term: "Homogeneous coordinates",
    key: "homogeneous-coordinates",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "A graphics trick: add a coordinate fixed at 1 so translations become matmuls. ML rarely uses this.",
    long: "<p><strong>Homogeneous coordinates</strong> are a trick from computer graphics: add an extra coordinate always set to 1, so translations can be expressed as matrix multiplications in the bigger space. A 2D point <code>(x, y)</code> becomes <code>(x, y, 1)</code> and a translation <code>+ (tx, ty)</code> becomes a 3×3 matrix.</p><p>ML rarely uses this — we keep the bias vector separate and use broadcasting instead. Mentioned so you recognize it in graphics code, not because you'll write it.</p>",
    example: "",
    related: ["affine", "bias-vector", "matmul"],
    flashcardFront: "What are homogeneous coordinates, and does ML use them?",
    flashcardBack: "A graphics trick: add a coordinate fixed at 1 so translations become matrix multiplications in a bigger space. ML almost never uses it — we keep the bias as a separate vector and broadcast instead.",
  },
  {
    term: "SVD (Singular Value Decomposition)",
    key: "svd",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Linear Algebra",
    short: "Factors any matrix as A = U Σ Vᵀ — rotation × non-negative scaling × rotation.",
    long: "<p>The <strong>Singular Value Decomposition</strong> factors <em>any</em> matrix (any shape) as <code>A = U Σ Vᵀ</code>: an orthogonal matrix <code>V</code> (rotation), then a diagonal non-negative matrix <code>Σ</code> (axis-aligned stretching), then another orthogonal matrix <code>U</code> (rotation). Every matrix is geometrically a rotation, a stretch, and another rotation.</p><p>SVD is the single most useful matrix decomposition in ML: it gives you PCA, low-rank approximation (compression), pseudo-inverses for rank-deficient regression, and solid numerical stability. Covered in depth in Lesson 4.</p>",
    example: "<pre><code>U, s, Vt = np.linalg.svd(A, full_matrices=False)</code></pre>",
    related: ["orthogonal", "diagonal-matrix", "singular-value", "pca", "low-rank"],
    flashcardFront: "What does SVD factor a matrix into, geometrically?",
    flashcardBack: "<code>A = U Σ Vᵀ</code>: a rotation (<code>Vᵀ</code>), then a non-negative diagonal stretch (<code>Σ</code>), then another rotation (<code>U</code>). Works for any shape. The most useful decomposition in ML.",
  },
  {
    term: "ReLU (Rectified Linear Unit)",
    key: "relu",
    module: "module-01",
    lesson: "lesson-02",
    lessonPath: "../module-01-math-foundations/lesson-02-matrix-transformations.html",
    category: "Deep Learning (forward ref)",
    short: "The nonlinearity max(0, x) slotted between affine layers in a neural network.",
    long: "<p><strong>ReLU</strong> (Rectified Linear Unit) is the nonlinearity <code>ReLU(x) = max(0, x)</code>. It's applied element-wise between affine layers in a neural network: <code>y = ReLU(Wx + b)</code>.</p><p>Why it matters: without a nonlinearity, stacking affine layers just collapses to one big affine layer — no expressiveness gained. ReLU breaks that collapse and lets deep nets represent curved, non-linear functions. Covered in depth in Module 5.</p>",
    example: "<pre><code>np.maximum(0, x)      # ReLU — element-wise</code></pre>",
    related: ["affine", "linear-transformation"],
    flashcardFront: "What does ReLU do and why is it needed?",
    flashcardBack: "<code>ReLU(x) = max(0, x)</code>, applied element-wise. Without a nonlinearity between layers, stacked affine layers collapse to a single affine layer — no extra power. ReLU breaks that collapse.",
  },

  // ===================== MODULE 1 — Linear Algebra & Calculus (Lesson 3) =====================
  {
    term: "Derivative",
    key: "derivative",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "How fast a function's output changes when you wiggle its input — slope of the tangent line.",
    long: "<p>The <strong>derivative</strong> of a function <code>f(x)</code> at a point is the limit of the output change over the input change as the input change shrinks to zero. Geometrically, it's the slope of the tangent line at that point. Written <code>f'(x)</code> or <code>df/dx</code>.</p><p>It tells you how the output responds to a tiny wiggle in the input. That one idea — tiny input change → tiny output change → ratio → limit — is the foundation of everything in the training-loop half of ML.</p>",
    example: "<pre><code># f(x) = x² → f'(x) = 2x\n# At x = 3, derivative is 6</code></pre>",
    related: ["slope", "rate-of-change", "gradient", "partial-derivative"],
    flashcardFront: "What is the derivative of a function, intuitively and geometrically?",
    flashcardBack: "How fast the output changes per unit change in the input, in the limit of an infinitesimally small input change. Geometrically: the slope of the tangent line at that point. Written <code>f'(x)</code> or <code>df/dx</code>.",
  },
  {
    term: "Slope",
    key: "slope",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "Rise over run. For a curve, it changes from point to point — that's what the derivative captures.",
    long: "<p><strong>Slope</strong> is rise over run — the change in output divided by the change in input along a line. For a straight line it's constant. For a curve it varies from point to point, and the derivative is the slope of the tangent line at each point.</p><p>This is the visual intuition behind the derivative: zoom in on a curve far enough and it looks straight; the slope of that straight-looking piece is <code>f'(x)</code>.</p>",
    example: "",
    related: ["derivative", "rate-of-change"],
    flashcardFront: "How are slope and derivative related?",
    flashcardBack: "The derivative at a point equals the slope of the tangent line there. For a straight line, slope is constant and equals the derivative everywhere. For a curve, both vary from point to point.",
  },
  {
    term: "Rate of change",
    key: "rate-of-change",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "How much output changes per unit change in input. Derivative = instantaneous rate of change.",
    long: "<p>The <strong>rate of change</strong> of a function is how much its output changes per unit change in its input. Over a finite interval, it's the average rate. The <strong>derivative</strong> is the <em>instantaneous</em> rate — the limit as the interval shrinks to zero.</p><p>Same concept as speed (distance/time): average speed over a trip vs. speedometer reading right now.</p>",
    example: "",
    related: ["derivative", "slope"],
    flashcardFront: "What's the difference between average rate of change and the derivative?",
    flashcardBack: "Average rate of change is the output change divided by the input change over a finite interval. The derivative is the limit as that interval shrinks to zero — the instantaneous rate of change at a single point.",
  },
  {
    term: "Stationary point",
    key: "stationary-point",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "A point where the derivative (or gradient) is zero. Could be a minimum, maximum, or saddle point.",
    long: "<p>A <strong>stationary point</strong> is any point where the derivative (1D) or gradient (multi-D) of a function is zero. Candidates include local minima, local maxima, and saddle points (flat in some directions, curved in others).</p><p>Gradient descent stalls at any stationary point because the update <code>w ← w − η·∇L</code> becomes <code>w ← w</code> when <code>∇L = 0</code>. In high dimensions (like a neural net's weight space), saddle points are far more common than true minima.</p>",
    example: "",
    related: ["derivative", "gradient", "minimum", "convergence"],
    flashcardFront: "What is a stationary point and why does it matter for gradient descent?",
    flashcardBack: "A point where the derivative (or gradient) is zero — could be a min, max, or saddle. Gradient descent stalls at any of them because the update becomes zero. Saddle points dominate in high-dimensional neural-net landscapes.",
  },
  {
    term: "Minimum",
    key: "minimum",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "A point where the function is lower than everything nearby. Derivative is zero there.",
    long: "<p>A <strong>minimum</strong> is a point where the function's value is lower than everything in some neighborhood around it. For a differentiable function, the derivative (or gradient) is zero at every minimum — but the converse isn't true (maxima and saddles also have zero derivative).</p><p>Training an ML model is fundamentally a search for a minimum of the loss function.</p>",
    example: "",
    related: ["local-minimum", "global-minimum", "stationary-point", "loss-function"],
    flashcardFront: "What characterizes a minimum of a differentiable function?",
    flashcardBack: "The function value is lower than everything in a neighborhood around the point, and the derivative (gradient) is zero there. Zero derivative is necessary but not sufficient — maxima and saddles also have it.",
  },
  {
    term: "Local minimum",
    key: "local-minimum",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "The lowest point in its immediate neighborhood — but not necessarily the overall lowest.",
    long: "<p>A <strong>local minimum</strong> is a point that's the lowest in its immediate neighborhood, but not necessarily the lowest over the function's entire domain. A function can have many local minima at different heights.</p><p>Gradient descent only guarantees convergence to a local minimum (at best), which is a genuine concern for non-convex losses like deep-net training. In practice, the local minima you find in deep nets are usually good enough — and there's empirical evidence most are near-global.</p>",
    example: "",
    related: ["minimum", "global-minimum", "convex-function", "gradient-descent"],
    flashcardFront: "What's the difference between a local minimum and a global minimum?",
    flashcardBack: "Local min: lowest in its immediate neighborhood. Global min: lowest over the entire domain. A function can have many local minima. Gradient descent only guarantees a local minimum — fine for convex losses, potentially worrying for deep nets.",
  },
  {
    term: "Global minimum",
    key: "global-minimum",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "The single lowest point over the function's entire domain.",
    long: "<p>The <strong>global minimum</strong> is the single lowest point of a function over its entire domain. For convex functions (bowl-shaped), the local minimum and global minimum coincide — any stationary point is the global minimum. For non-convex functions (like deep-net losses), there can be many local minima, and the global one is generally unreachable by gradient descent alone.</p>",
    example: "",
    related: ["minimum", "local-minimum", "convex-function"],
    flashcardFront: "When do local and global minima coincide?",
    flashcardBack: "When the function is convex. Convex functions have exactly one minimum (which is both local and global). Non-convex functions (deep-net losses) can have many local minima at different heights.",
  },
  {
    term: "Convex function",
    key: "convex-function",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "Bowl-shaped: a line segment between any two points on the graph stays above or on the graph.",
    long: "<p>A function is <strong>convex</strong> if, for any two points on its graph, the line segment connecting them stays on or above the graph. Informally: bowl-shaped. Every convex function has a unique global minimum, and gradient descent is guaranteed to reach it.</p><p>Linear regression's mean-squared-error loss is convex — that's why OLS has a closed-form solution. Logistic regression's log-loss is also convex. Deep-net losses are generally non-convex, which is why training them is harder and more empirical.</p>",
    example: "",
    related: ["global-minimum", "local-minimum", "loss-function", "gradient-descent"],
    flashcardFront: "What's the key optimization property of a convex function?",
    flashcardBack: "Any local minimum is also the global minimum, and gradient descent is guaranteed to reach it. MSE (linear regression) and log-loss (logistic regression) are convex; deep-net losses generally aren't.",
  },
  {
    term: "Chain rule",
    key: "chain-rule",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "Derivative of a composition: (f ∘ g)'(x) = f'(g(x)) · g'(x). Applied layer by layer, you get backprop.",
    long: "<p>The <strong>chain rule</strong> is the calculus rule for differentiating composed functions: <code>(f ∘ g)'(x) = f'(g(x)) · g'(x)</code>. In words: differentiate the outer function at the inner function's value, then multiply by the inner function's derivative.</p><p>Apply the chain rule layer by layer through a neural network and you get backpropagation — just systematic bookkeeping of chain-rule products. This is the single most important calculus rule in ML.</p>",
    example: "<pre><code># d/dx sin(x²) = cos(x²) · 2x\n# outer'(inner) · inner'</code></pre>",
    related: ["derivative", "backpropagation", "partial-derivative"],
    flashcardFront: "State the chain rule and say why it matters in ML.",
    flashcardBack: "<code>(f ∘ g)'(x) = f'(g(x)) · g'(x)</code>. Applied layer by layer through a neural network, it gives you backpropagation — the algorithm that computes every parameter's gradient.",
  },
  {
    term: "Partial derivative",
    key: "partial-derivative",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "Derivative of a multi-variable function with respect to one variable, holding the others fixed.",
    long: "<p>A <strong>partial derivative</strong> of a multi-variable function <code>f(x, y, ...)</code> with respect to one variable (say <code>x</code>) is its derivative when all the other variables are held fixed. Written <code>∂f/∂x</code> with a rounded \"del\" symbol to distinguish it from the single-variable <code>df/dx</code>.</p><p>Partials are the building blocks of the gradient: the gradient is just the vector of all the partial derivatives, one per input variable.</p>",
    example: "<pre><code># f(x, y) = x² + 3xy + y²\n# ∂f/∂x = 2x + 3y    (treat y as constant)\n# ∂f/∂y = 3x + 2y    (treat x as constant)</code></pre>",
    related: ["derivative", "gradient", "chain-rule"],
    flashcardFront: "What's a partial derivative, and how does it relate to the gradient?",
    flashcardBack: "The derivative of a multi-variable function with respect to one variable, holding the others fixed. Written <code>∂f/∂x</code>. The gradient is just the vector of all partial derivatives, one per input.",
  },
  {
    term: "Gradient",
    key: "gradient",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Calculus",
    short: "Vector of all partial derivatives; points in the direction of steepest ascent.",
    long: "<p>The <strong>gradient</strong> <code>∇f</code> of a multi-variable function is the vector of all its partial derivatives — same shape as the input. At any point, it points in the direction of steepest ascent of the function, and its magnitude is how steep that ascent is.</p><p>In ML, we want to minimize a loss, so we go in the opposite direction: <code>w ← w − η·∇L(w)</code>. That's gradient descent — the single most important algorithm in training neural nets.</p>",
    example: "<pre><code># f(x, y) = x² + y²\n# ∇f = [2x, 2y]   at (1, 2) → [2, 4]</code></pre>",
    related: ["partial-derivative", "gradient-descent", "derivative", "loss-function"],
    flashcardFront: "What is the gradient of a function, and what direction does it point?",
    flashcardBack: "The vector of all partial derivatives, same shape as the input. At any point, it points in the direction of steepest ascent, with magnitude equal to the steepness. Gradient descent goes in the opposite direction.",
  },
  {
    term: "Loss function",
    key: "loss-function",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Classical ML (forward ref)",
    short: "A scalar function of the model's weights that measures how wrong its predictions are.",
    long: "<p>A <strong>loss function</strong> <code>L(w)</code> is a scalar-valued function of the model's weights that measures how wrong its predictions are on the training data. Lower is better. Training a model means searching the weight space for a <code>w</code> that minimizes the loss.</p><p>Examples: mean squared error for regression, cross-entropy for classification. Covered in detail in Module 3.</p>",
    example: "<pre><code># Mean squared error\nL = np.mean((y_pred - y_true) ** 2)</code></pre>",
    related: ["gradient", "gradient-descent", "minimum", "convex-function"],
    flashcardFront: "What is a loss function?",
    flashcardBack: "A scalar function of the model's weights measuring how wrong its predictions are on the training data. Lower is better. Training = minimizing this function. Examples: MSE (regression), cross-entropy (classification).",
  },
  {
    term: "Gradient descent",
    key: "gradient-descent",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Classical ML",
    short: "Iterative optimizer: compute gradient, step against it, repeat. w ← w − η·∇L(w).",
    long: "<p><strong>Gradient descent</strong> is the iterative optimization algorithm behind essentially every neural network in existence. Start somewhere, compute the gradient of the loss with respect to the weights, take a small step in the opposite direction, repeat until convergence: <code>w ← w − η·∇L(w)</code>.</p><p>The learning rate <code>η</code> controls step size. Variants (SGD, momentum, Adam, RMSprop) add tricks like per-parameter scaling and momentum buffers but keep the same core idea.</p>",
    example: "<pre><code>for step in range(n_steps):\n    grad = compute_gradient(w)\n    w = w - learning_rate * grad</code></pre>",
    related: ["gradient", "learning-rate", "loss-function", "optimizer", "convergence"],
    flashcardFront: "Write the gradient-descent update rule and name its hyperparameter.",
    flashcardBack: "<code>w ← w − η · ∇L(w)</code>. The hyperparameter <code>η</code> is the learning rate (step size). Variants (SGD, Adam, RMSprop) add momentum or per-parameter scaling but keep this core structure.",
  },
  {
    term: "Learning rate",
    key: "learning-rate",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Classical ML",
    short: "The step-size parameter η in gradient descent. Too large → diverges. Too small → crawls.",
    long: "<p>The <strong>learning rate</strong> <code>η</code> (eta) is the step size in gradient descent: how far to move along the negative gradient at each step. Too large and training diverges (loss blows up to infinity or NaN). Too small and it crawls — takes forever to get anywhere useful.</p><p>Typical starting values: <code>1e-3</code> for Adam, <code>1e-2</code> to <code>1e-1</code> for plain SGD. Tuning the learning rate is usually the single highest-impact knob in neural-net training.</p>",
    example: "",
    related: ["gradient-descent", "optimizer", "convergence"],
    flashcardFront: "What happens if the learning rate is too large? Too small?",
    flashcardBack: "Too large: training diverges — loss goes to infinity or NaN. Too small: training crawls and wastes compute. Typical defaults: <code>1e-3</code> for Adam, <code>1e-2</code> to <code>1e-1</code> for SGD.",
  },
  {
    term: "Convergence",
    key: "convergence",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Classical ML",
    short: "When an iterative optimizer stops making meaningful progress — gradient near zero, loss plateaued.",
    long: "<p><strong>Convergence</strong> is when an iterative optimizer has stopped making meaningful progress — the gradient is near zero, the loss change is under some threshold, or a max-iteration budget has been hit. In ML, \"converged\" usually means \"the loss stopped decreasing appreciably.\"</p><p>Convergence to a local minimum is what gradient descent guarantees (under mild conditions). For convex losses that's also the global minimum; for non-convex losses you hope you landed somewhere good enough.</p>",
    example: "",
    related: ["gradient-descent", "stationary-point", "loss-function", "learning-rate"],
    flashcardFront: "What does 'convergence' mean for an iterative optimizer?",
    flashcardBack: "The optimizer has stopped making meaningful progress: gradient near zero, loss change below threshold, or max iterations hit. For convex losses, convergence means reaching the global minimum; otherwise, a local one.",
  },
  {
    term: "Vanishing gradients",
    key: "vanishing-gradients",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Deep Learning",
    short: "Gradients shrink toward zero through many layers — early layers stop learning.",
    long: "<p><strong>Vanishing gradients</strong> are a training pathology where gradient values shrink toward zero as the chain rule propagates them through many layers. Multiplying a lot of small numbers together gets you a tinier number — and with sigmoid or tanh activations whose derivatives max out well below 1, this happens fast.</p><p>The symptom: early layers barely move during training. Mitigations: ReLU activations (gradient is 1 for positive inputs), residual connections (add shortcut paths), and careful initialization. Covered in Module 5.</p>",
    example: "",
    related: ["gradient", "chain-rule", "backpropagation", "relu"],
    flashcardFront: "What causes vanishing gradients and how is it mitigated?",
    flashcardBack: "Chain-rule multiplies gradients through many layers; with activations whose derivatives are &lt;1 (sigmoid, tanh), products shrink toward zero and early layers stop learning. Fix: ReLU, residual connections, better initialization.",
  },
  {
    term: "Backpropagation",
    key: "backpropagation",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Deep Learning",
    short: "The algorithm that applies the chain rule through a neural net to compute gradients of the loss.",
    long: "<p><strong>Backpropagation</strong> is the algorithm that computes the gradient of a loss with respect to every parameter in a neural network. It does a forward pass to compute the loss, then walks backward layer by layer applying the chain rule, reusing intermediate quantities along the way.</p><p>Not magic — just systematic bookkeeping of chain-rule products. The efficiency trick is that it avoids recomputing shared sub-expressions, so the total cost is roughly the same as a forward pass. Built as a framework feature (autograd) in PyTorch, TensorFlow, and JAX. Covered in Module 5.</p>",
    example: "<pre><code>loss.backward()   # autograd does backprop for you</code></pre>",
    related: ["chain-rule", "autograd", "gradient", "vanishing-gradients"],
    flashcardFront: "What does backpropagation compute, and what rule does it apply?",
    flashcardBack: "It computes the gradient of the loss with respect to every parameter in a neural net. It walks backward from output to input, applying the chain rule layer by layer and reusing shared sub-expressions.",
  },
  {
    term: "Autograd",
    key: "autograd",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Deep Learning",
    short: "Automatic differentiation: a framework tracks the forward pass and computes exact gradients for you.",
    long: "<p><strong>Autograd</strong> (automatic differentiation) is a framework feature — PyTorch, TensorFlow, JAX — that records the computation graph of your forward pass and computes exact gradients when you call <code>.backward()</code>. It applies the chain rule automatically; you never hand-derive gradients.</p><p>Not numerical differentiation (finite differences) and not symbolic differentiation (algebra). It's a third thing: it runs your actual code and tracks operations, so it handles arbitrary control flow correctly.</p>",
    example: "<pre><code>x = torch.tensor(3.0, requires_grad=True)\ny = x**2 + 2*x\ny.backward()\nx.grad   # tensor(8.) — dy/dx at x=3</code></pre>",
    related: ["backpropagation", "chain-rule", "gradient", "optimizer"],
    flashcardFront: "What is autograd and how is it different from numerical or symbolic differentiation?",
    flashcardBack: "A framework feature (PyTorch, TF, JAX) that records the forward pass and applies the chain rule automatically on <code>.backward()</code>. Not finite differences (numerical) and not algebraic manipulation (symbolic) — it runs your real code and tracks ops.",
  },
  {
    term: "Optimizer",
    key: "optimizer",
    module: "module-01",
    lesson: "lesson-03",
    lessonPath: "../module-01-math-foundations/lesson-03-derivatives-gradients.html",
    category: "Deep Learning",
    short: "The object in a training loop that applies the update rule — SGD, Adam, RMSprop, etc.",
    long: "<p>An <strong>optimizer</strong> is the object in a training loop that applies the weight-update rule. It holds state (learning rate, momentum buffers, per-parameter scale factors) and, when you call <code>.step()</code>, applies <code>w ← w − η·∇L</code> plus whatever algorithm-specific tricks it adds.</p><p>Common ones: <code>SGD</code> (plain gradient descent), <code>SGD with momentum</code> (exponential moving average of gradients), <code>Adam</code> (per-parameter adaptive learning rates — the de-facto default). Covered in Module 5.</p>",
    example: "<pre><code>optimizer = torch.optim.Adam(model.parameters(), lr=1e-3)\n...\noptimizer.zero_grad()\nloss.backward()\noptimizer.step()</code></pre>",
    related: ["gradient-descent", "learning-rate", "autograd"],
    flashcardFront: "What does a PyTorch optimizer do, and what's the canonical training-loop sequence?",
    flashcardBack: "Holds optimizer state (lr, momentum) and applies the update rule on <code>.step()</code>. Canonical sequence: <code>optimizer.zero_grad()</code> → <code>loss.backward()</code> → <code>optimizer.step()</code>.",
  },

  // ===================== MODULE 1 — Linear Algebra & Calculus (Lesson 4) =====================
  {
    term: "Eigenvector",
    key: "eigenvector",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A direction a square matrix only stretches — doesn't rotate. Satisfies Av = λv.",
    long: "<p>An <strong>eigenvector</strong> of a square matrix <code>A</code> is a non-zero vector <code>v</code> that <code>A</code> only stretches (or flips), never rotates. It satisfies <code>A @ v = λ · v</code> for some scalar <code>λ</code> (the eigenvalue).</p><p>Most vectors are <em>not</em> eigenvectors — that's exactly what makes eigenvectors informative. They reveal the intrinsic axes of the transformation: the directions that don't get tangled up when you apply <code>A</code>. These special axes are the backbone of PCA.</p>",
    example: "<pre><code>eigenvalues, eigenvectors = np.linalg.eig(A)\n# each column of eigenvectors is one v</code></pre>",
    related: ["eigenvalues", "square-matrix", "pca", "principal-component", "normalize-vec"],
    flashcardFront: "What is an eigenvector?",
    flashcardBack: "A non-zero vector <code>v</code> such that <code>A @ v = λ · v</code> — a direction the square matrix <code>A</code> only stretches (or flips) without rotating. Most vectors are not eigenvectors; those that are reveal <code>A</code>'s intrinsic axes.",
  },
  {
    term: "Characteristic polynomial",
    key: "characteristic-polynomial",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "The polynomial det(A − λI) = 0 whose roots are A's eigenvalues.",
    long: "<p>The <strong>characteristic polynomial</strong> of a square matrix <code>A</code> is the polynomial <code>det(A − λI)</code> in the variable <code>λ</code>. Its roots are exactly the eigenvalues of <code>A</code>. For a 2×2 matrix it's a quadratic you can solve by hand.</p><p>Useful for pen-and-paper eigenvalue problems on toy matrices, but numerically unstable for anything larger — real libraries use iterative algorithms (QR, power iteration). You'll see it in the math but rarely in the code.</p>",
    example: "<pre><code># For a 2×2 matrix A = [[a, b], [c, d]]\n# det(A − λI) = (a−λ)(d−λ) − bc = 0</code></pre>",
    related: ["eigenvalues", "eigenvector", "determinant"],
    flashcardFront: "What equation gives you the eigenvalues of a matrix?",
    flashcardBack: "<code>det(A − λI) = 0</code> — the characteristic polynomial. Its roots are the eigenvalues. Works by hand for 2×2, but numerically unstable for bigger matrices; real libraries use iterative algorithms.",
  },
  {
    term: "Square matrix",
    key: "square-matrix",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A matrix with the same number of rows and columns. Eigenvalues live here.",
    long: "<p>A <strong>square matrix</strong> has the same number of rows and columns. Eigenvalues and eigenvectors are only defined for square matrices, because the equation <code>A @ v = λ · v</code> requires <code>A @ v</code> and <code>v</code> to live in the same space — which happens only when <code>A</code> is square.</p><p>SVD, by contrast, is defined for any shape of matrix — one reason it's more general and more useful in practice.</p>",
    example: "",
    related: ["matrix", "eigenvalues", "svd"],
    flashcardFront: "Why are eigenvalues only defined for square matrices?",
    flashcardBack: "Because <code>A @ v = λ · v</code> requires the output <code>A @ v</code> to live in the same space as the input <code>v</code>. Only a square matrix maps a space to itself. SVD generalizes eigen-decomposition to any shape.",
  },
  {
    term: "Orthogonal matrix",
    key: "orthogonal",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A square matrix Q with QᵀQ = I — columns are orthonormal; geometrically, a pure rotation.",
    long: "<p>An <strong>orthogonal matrix</strong> is a square matrix <code>Q</code> whose columns (and rows) are mutually perpendicular unit vectors — equivalently, <code>Qᵀ Q = I</code>, so <code>Qᵀ = Q⁻¹</code>. Geometrically, it's a pure rotation, possibly combined with a reflection. No stretching, no shearing; lengths and angles are preserved.</p><p>The <code>U</code> and <code>V</code> in SVD are orthogonal matrices — that's what makes the SVD interpretation \"rotation, scale, rotation\" so clean.</p>",
    example: "<pre><code>Q.T @ Q   # ≈ identity for orthogonal Q</code></pre>",
    related: ["orthonormal", "svd", "identity-matrix", "square-matrix"],
    flashcardFront: "What defines an orthogonal matrix, and what's its geometric meaning?",
    flashcardBack: "<code>Qᵀ Q = I</code> — columns are mutually perpendicular unit vectors, and <code>Qᵀ = Q⁻¹</code>. Geometrically a pure rotation (possibly with a reflection). Preserves lengths and angles. <code>U</code> and <code>V</code> in SVD are orthogonal.",
  },
  {
    term: "Orthonormal",
    key: "orthonormal",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A set of vectors that are mutually perpendicular AND each have length 1.",
    long: "<p><strong>Orthonormal</strong> = orthogonal (mutually perpendicular) <em>and</em> normalized (each vector has length 1). Rows and columns of an orthogonal matrix are orthonormal. Principal components are orthonormal — that's why they define a clean rotated coordinate system for the data.</p>",
    example: "",
    related: ["orthogonal", "unit-vector", "principal-component", "normalize-vec"],
    flashcardFront: "What does 'orthonormal' mean?",
    flashcardBack: "Orthogonal (mutually perpendicular) AND each vector has length 1. Rows and columns of an orthogonal matrix are orthonormal. Principal components are orthonormal — they give a clean rotated coordinate system.",
  },
  {
    term: "Singular value",
    key: "singular-value",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A non-negative diagonal entry of Σ in SVD — the stretch factor along one axis.",
    long: "<p>A <strong>singular value</strong> is one of the non-negative diagonal entries of <code>Σ</code> in the SVD <code>A = U Σ Vᵀ</code>. Each represents how much <code>A</code> stretches along a particular axis. Singular values generalize eigenvalues to non-square (and even square) matrices.</p><p>Libraries always return them sorted descending, so <code>s[0]</code> is the strongest axis. Ratios like <code>s[0] / s[-1]</code> (the condition number) tell you how numerically well-behaved the matrix is.</p>",
    example: "<pre><code>U, s, Vt = np.linalg.svd(A)\ns       # singular values, sorted descending</code></pre>",
    related: ["svd", "singular-vector", "eigenvalues", "diagonal-matrix"],
    flashcardFront: "What is a singular value, and how are they ordered?",
    flashcardBack: "A non-negative diagonal entry of <code>Σ</code> in the SVD <code>A = UΣVᵀ</code>. Represents the stretch factor along one axis. Always returned sorted descending, so <code>s[0]</code> is the strongest axis.",
  },
  {
    term: "Singular vector",
    key: "singular-vector",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A column of U (left) or V (right) in SVD — an orthonormal axis where A acts as pure scaling.",
    long: "<p>A <strong>singular vector</strong> is a column of <code>U</code> (a <em>left</em> singular vector) or a column of <code>V</code> (a <em>right</em> singular vector) in the SVD <code>A = U Σ Vᵀ</code>. The right singular vectors are the orthonormal input axes along which <code>A</code> acts as pure scaling; the left ones are the corresponding orthonormal output axes.</p><p>In PCA, the right singular vectors of the centered data matrix are the principal components.</p>",
    example: "",
    related: ["svd", "singular-value", "orthonormal", "principal-component"],
    flashcardFront: "What are left and right singular vectors?",
    flashcardBack: "Columns of <code>U</code> (left) and <code>V</code> (right) in <code>A = UΣVᵀ</code>. Right singular vectors = orthonormal input axes <code>A</code> scales purely; left = orthonormal output axes. In PCA on centered data, right singular vectors are the principal components.",
  },
  {
    term: "Rank",
    key: "rank",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "The number of linearly independent columns (or rows) — equals the count of nonzero singular values.",
    long: "<p>The <strong>rank</strong> of a matrix is the number of linearly independent columns — equivalently, the number of linearly independent rows, or the number of nonzero singular values in its SVD. It's the \"true\" dimensionality of the information the matrix carries.</p><p>A matrix can have shape <code>(1000, 50)</code> but rank 3 if the 50 columns are all expressible as combinations of 3 underlying directions. That's the whole reason dimensionality reduction works — real data tends to be approximately low-rank.</p>",
    example: "<pre><code>np.linalg.matrix_rank(X)   # count of nonzero singular values (within tolerance)</code></pre>",
    related: ["low-rank", "rank-deficient", "svd", "singular-value"],
    flashcardFront: "What is the rank of a matrix, and how is it related to SVD?",
    flashcardBack: "The number of linearly independent columns (or rows) — equivalently, the count of nonzero singular values in the SVD. Tells you the true dimensionality of the information in the matrix.",
  },
  {
    term: "Low-rank",
    key: "low-rank",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Linear Algebra",
    short: "A matrix whose rank is much smaller than its dimensions. Real datasets usually are.",
    long: "<p>A matrix is <strong>low-rank</strong> when its rank is much smaller than its dimensions — say, a <code>1000 × 50</code> matrix with rank 5. Most real-world datasets are approximately low-rank: a small number of latent factors explain most of the variance, and the rest is noise or near-duplication.</p><p>This is what makes SVD-based compression, PCA, and recommender systems work. Truncating the SVD to its top-<code>k</code> singular values is the best rank-<code>k</code> approximation of the original matrix (Eckart–Young theorem).</p>",
    example: "",
    related: ["rank", "svd", "pca", "principal-component"],
    flashcardFront: "Why are most real datasets approximately low-rank, and why does it matter?",
    flashcardBack: "A few latent factors explain most of the variance; the rest is noise or near-duplication. This is what makes SVD-based compression, PCA, and recommender systems work — truncated SVD is the best low-rank approximation.",
  },
  {
    term: "Principal component",
    key: "principal-component",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Classical ML",
    short: "An orthonormal direction along which data has maximal (remaining) variance.",
    long: "<p>A <strong>principal component</strong> is an orthonormal direction along which your data has maximal (remaining) variance. The first PC is the direction of maximum variance; the second is the direction of maximum variance orthogonal to the first; and so on. They form a clean rotated coordinate system for the data, ordered by informativeness.</p><p>Mathematically: the <code>i</code>-th principal component is the <code>i</code>-th right singular vector of the centered data matrix (equivalently, the <code>i</code>-th eigenvector of the covariance matrix). Used throughout Module 4.</p>",
    example: "<pre><code>from sklearn.decomposition import PCA\npca = PCA(n_components=2).fit(X)\npca.components_   # rows are the top-2 principal components</code></pre>",
    related: ["pca", "orthonormal", "singular-vector", "covariance-matrix", "variance"],
    flashcardFront: "What is a principal component, mathematically?",
    flashcardBack: "An orthonormal direction of maximal (remaining) variance in the data. Equivalently: a right singular vector of the centered data matrix, or an eigenvector of the covariance matrix. PCs are ordered by decreasing variance.",
  },
  {
    term: "Variance",
    key: "variance",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Classical ML",
    short: "A measure of how spread out a set of numbers is around their mean.",
    long: "<p><strong>Variance</strong> measures how spread out a set of numbers is around their mean: the average of squared deviations from the mean. High variance = scattered; low variance = clustered tightly.</p><p>In PCA, each principal component comes with a variance telling you how much of the data's spread it captures. The first few PCs usually capture most of the variance — that's why you can throw away the rest without losing much information. Formal definition and properties in Module 2.</p>",
    example: "<pre><code>np.var(x)               # population variance\nnp.var(x, ddof=1)       # sample variance (divide by n-1)</code></pre>",
    related: ["principal-component", "pca", "covariance-matrix", "standardize"],
    flashcardFront: "What does variance measure, and what role does it play in PCA?",
    flashcardBack: "The spread of numbers around their mean (average squared deviation). In PCA, each principal component has an associated variance — how much of the data's spread it captures. Top PCs capture most of it.",
  },
  {
    term: "Covariance matrix",
    key: "covariance-matrix",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Classical ML",
    short: "A d×d symmetric matrix summarizing how each pair of features varies together.",
    long: "<p>The <strong>covariance matrix</strong> <code>C</code> of a dataset is a <code>d × d</code> symmetric matrix where <code>C[i, j]</code> is the covariance between feature <code>i</code> and feature <code>j</code>. Diagonal entries are each feature's variance; off-diagonal entries show how pairs move together.</p><p>Formula on centered data: <code>C = (1/(n−1)) · X_cᵀ X_c</code>. Its eigenvectors are the principal components; its eigenvalues are the variances along each PC. Covered formally in Module 2.</p>",
    example: "<pre><code>X_c = X - X.mean(axis=0)\nC = (X_c.T @ X_c) / (len(X) - 1)\n# or: np.cov(X, rowvar=False)</code></pre>",
    related: ["variance", "pca", "principal-component", "eigenvalues"],
    flashcardFront: "What is the covariance matrix, and how does it connect to PCA?",
    flashcardBack: "A <code>d × d</code> symmetric matrix where entry <code>[i, j]</code> is the covariance between features <code>i</code> and <code>j</code>. Its eigenvectors are the principal components; its eigenvalues are the variances along them.",
  },
  {
    term: "Standardize",
    key: "standardize",
    module: "module-01",
    lesson: "lesson-04",
    lessonPath: "../module-01-math-foundations/lesson-04-eigenvalues-svd.html",
    category: "Classical ML",
    short: "Rescale each feature to zero mean and unit variance. Required before PCA on mixed-scale features.",
    long: "<p>To <strong>standardize</strong> a dataset is to rescale each feature so it has zero mean and unit variance: subtract the column mean, divide by the column standard deviation. Sklearn's <code>StandardScaler</code> does this.</p><p>Required before PCA when features have different natural scales (dollars vs. years vs. counts). Otherwise the biggest-scale feature hijacks the first principal component regardless of its actual signal — it'll look informative only because its numbers are bigger. Standardizing puts every feature on equal footing.</p>",
    example: "<pre><code>from sklearn.preprocessing import StandardScaler\nX_std = StandardScaler().fit_transform(X)</code></pre>",
    related: ["variance", "pca", "diagonal-matrix", "normalize-vec"],
    flashcardFront: "What does it mean to standardize features, and why do it before PCA?",
    flashcardBack: "Rescale each feature to zero mean and unit variance (subtract mean, divide by std). Before PCA: without standardizing, the largest-scale feature dominates PC1 regardless of signal. <code>StandardScaler</code> in sklearn.",
  },
];

// Convenience: lookup map keyed by term key
window.ML_GLOSSARY_MAP = Object.fromEntries(
  window.ML_GLOSSARY.map(entry => [entry.key, entry])
);

// Convenience: modules present in the data
window.ML_GLOSSARY_MODULES = Array.from(
  new Set(window.ML_GLOSSARY.map(e => e.module))
).sort();
